{
  "name": "universal-dev-macos",
  "dockerComposeFile": "docker-compose.yml",
  "service": "dev",
  "workspaceFolder": "/workspace",
  "remoteUser": "dev",
  "shutdownAction": "stopCompose",
  "overrideCommand": true,
  "postCreateCommand": "bash -lc 'git --version && docker --version && gh --version && kubectl version --client >/dev/null && python3 --version && node --version && go version && rustc --version && java -version >/dev/null && codex --help >/dev/null && claude --help >/dev/null'",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      },
      "extensions": [
        "ms-azuretools.vscode-docker",
        "ms-kubernetes-tools.vscode-kubernetes-tools",
        "github.vscode-github-actions",
        "github.copilot"
      ]
    }
  }
}
